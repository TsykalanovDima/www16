function mainFunction(a,b) {
	return a ** b	
}

function exponentiation(callback) {
	let a = +prompt('nubmer 1')

	let b = +prompt('number 2')

	return callback(a,b)
}

alert (exponentiation(mainFunction))

                                            2


function mainFunction(a,b) {
	return a + b	
}

function exponentiation(callback) {
	let a = +prompt('nubmer 1')

	let b = +prompt('number 2')

	return callback(a,b)
}

alert (exponentiation(mainFunction))
 
                                                  3

function mainFunction(a,b) {
	return a / b	
}

function exponentiation(callback) {
	let a = +prompt('nubmer 1')

	let b = +prompt('number 2')

	return callback(a,b)
}

alert (exponentiation(mainFunction))												  



                                               4
function mainFunction(a,b) {
	return a % b	
}

function exponentiation(callback) {
	let a = +prompt('nubmer 1')

	let b = +prompt('number 2')

	return callback(a,b)
}

alert (exponentiation(mainFunction))												  